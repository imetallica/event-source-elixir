defmodule Es.Commands.CreateOrderTest do
  use ExUnit.Case

  describe "CreateOrderCommand validation" do
    test "succeds when payload is correct."

    test "fails an order where the delivery date is earlier than the order date."

    test "fails when items have zero quantities."
  end
end
